import { StyleSheet } from "react-native";
import { colors } from "../../theme";

export const styles = StyleSheet.create({
    profileNoPhotoContainer: {
        height: 60,
        width: 60,
        backgroundColor: colors.grey[100],
        borderRadius: 50,
        alignItems: "center",
        justifyContent: "center",
    },
});
